{
  "mcpServers": {
    "my-first-agent": {
      "command": "node",
      "args": ["src/mcp-server/index.js"],
      "env": {
        "GOOGLE_API_KEY": "${GOOGLE_API_KEY}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
